{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.nomSalle) }}
    {{ form_row(form.date) }}
    {{ form_row(form.adresse) }}
    {# {{ form_row(form.plan) }}
    {{ form_row(form.imageTicket) }} #}
    <button class="btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
